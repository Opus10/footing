.. _cli:

Footing CLI
===========

The main ``footing`` command and its subcommands are listed below. Note that
``--help`` can be given as an argument to any of these commands to print
out help on the command line.

.. click:: footing.cli:main
    :prog: footing
    :show-nested:
